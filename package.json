{
  "name": "ekai-gateway",
  "version": "0.1.0-beta.1",
  "description": "Ekai Gateway - Multi-provider AI request routing with dashboard",
  "private": true,
  "workspaces": ["gateway", "ui/dashboard"],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently \"npm run start:gateway\" \"npm run start:ui\"",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules",
    "test": "npm run test --workspace=gateway",
    "dev:gateway": "npm run dev --workspace=gateway",
    "dev:ui": "npm run dev --workspace=ui/dashboard",
    "start:gateway": "npm run start --workspace=gateway",
    "start:ui": "npm run start --workspace=ui/dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "ai",
    "proxy",
    "gateway",
    "dashboard",
    "openai",
    "anthropic",
    "openrouter"
  ],
  "author": "Ekai Labs"
}
