# Required — your OpenRouter API key
OPENROUTER_API_KEY=your_key_here

# Memory service URL (optional, defaults to http://localhost:4005)
# MEMORY_URL=http://localhost:4005

# Server port (optional, defaults to 4010)
# PORT=4010
